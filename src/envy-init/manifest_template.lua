-- envy.lua - Project manifest
-- @envy version "@@ENVY_VERSION@@"
-- @envy cache "/path/to/cache"      -- optional: override cache location
-- @envy mirror "https://mirror.example.com"  -- optional: override download mirror

PACKAGES = {
    -- Add packages here, e.g.:
    -- "local.python@v1",
    -- { recipe = "arm.gcc@v2", source = "https://...", options = { version = "13.2.0" } },
}
