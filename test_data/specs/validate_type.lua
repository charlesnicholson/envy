IDENTITY = "test.validate_type@v1"

VALIDATE = function(opts)
  return 123
end

CHECK = function(project_root, options) return true end
INSTALL = function(install_dir, stage_dir, fetch_dir, tmp_dir, options) end
